# Copy to .env and fill in. NEVER commit .env.

# --- NVIDIA build platform (free API key from build.nvidia.com) ---
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Optional overrides:
# NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
# NVIDIA_MODEL=nvidia/nemotron-3-ultra-550b-a55b

# --- Gmail (use a 16-char App Password, NOT your real password) ---
# Generate at: https://myaccount.google.com/apppasswords
GMAIL_ADDRESS=you@gmail.com
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx
# Where to send (defaults to GMAIL_ADDRESS if blank):
EMAIL_TO=

# --- Behavior ---
# Deload every Nth session (counted from last deload):
DELOAD_EVERY=5
# Force a specific day for manual test runs (monday/friday). Leave blank in prod.
FORCE_DAY=
# History file path (mounted volume in Docker):
HISTORY_PATH=/data/history.json
# Container timezone so cron fires at 5am LOCAL, not UTC:
TZ=America/New_York

# --- Strava (optional but recommended — feeds training load into workout design) ---
# Register a free app at: https://www.strava.com/settings/api
#   App Name: coach_claude  |  Website: http://localhost  |  Callback domain: localhost
# Then run: python -m src.setup_strava
# to do the one-time OAuth flow and get your refresh token.
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=
